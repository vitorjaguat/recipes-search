//This module contains and exports variables that are used several times all over the app. If the variables change in the future, for example the url for the API, then we only have to update it here.

export const API_URL = 'https://forkify-api.herokuapp.com/api/v2/recipes/';

export const TIMEOUT_SEC = 10;

export const RES_PER_PAGE = 10;